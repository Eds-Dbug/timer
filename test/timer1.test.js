const expect = require('chai').expect;
    
describe('#test1 the path of light', () => {
  xit('It should just not beep at all and end immediately since no beeps should get scheduled.',() => {
    //No numbers are provided: Easy. 
  })

  xit("Ignore/skip any numbers that are negative. We can't schedule anything in the past.",() => {
    //An input is a negative number: 
  })


  xit("Ignore/skip these as well, instead of attempting to call setTimeout with a non-number.",() => {
    //An input is not a number: 
  })



})
    
    
    
    
